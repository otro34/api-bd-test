class Profesor {
    constructor(profesor) {
        if (!profesor) return

        this.id = program.id,
        this.nombre = program.nombre,
        this.apellido = program.apellido,
        this.categoria = program.categoria,
        this.grado = program.grado

    }
}

export default Profesor